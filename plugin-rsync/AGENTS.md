# plugin-rsync

User-scope only. Do not add `plugin-rsync@bytedesk` to a repository's `.claude/settings.json`.

`plugin-rsync` with no arguments refreshes every installed bytedesk marketplace plugin. Names are comma-separated. Source of truth is the marketplace git checkout; destinations are host caches.
