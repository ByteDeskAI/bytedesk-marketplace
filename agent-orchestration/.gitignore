node_modules
.npm/
*.log
native/windows/bin/
native/windows/obj/

# Include the plugin-local generated brand assets despite the root PNG ignore.
!.context/design-system/**/*.png

# graft's local graph cache — regenerable, not committed (run `graft build`).
/graft/
