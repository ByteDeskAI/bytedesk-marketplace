#!/bin/sh
set -eu

ROOT="$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)"
CLAUDE_CODE_EXECUTABLE="${CLAUDE_CODE_EXECUTABLE:-$(command -v claude)}"
export CLAUDE_CODE_EXECUTABLE
exec node "$ROOT/dist/claude-agent-acp.mjs" "$@"
