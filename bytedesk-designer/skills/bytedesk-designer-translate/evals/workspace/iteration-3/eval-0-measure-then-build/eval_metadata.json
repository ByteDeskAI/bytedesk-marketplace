{
  "eval_id": 0,
  "eval_name": "measure-then-build",
  "prompt": "Here's the mockup our designer approved for the failed-render state of the studio app: ~/Pictures/claude-design/runs/2026-08-28-designer-studio-mockups/images/r3-s5.png (it's a 1280x800 window). Our design authority is at ~/Documents/GitHub/ByteDeskAI/design-system. Build me the real HTML for that state so it matches the mockup.",
  "assertions": [
    "Wrote translate/SPEC.md containing at least ten numeric measurements in logical pixels before the first Codex build",
    "SPEC.md records the invented-element decision explicitly \u2014 either naming what the mockup invented and must not be built, or stating that nothing structural was invented",
    "The surface HTML loads a vendored token stylesheet and contains no hex or rgb() colour literals",
    "Codex wrote the surface (a translate/prompts/*-r1.txt hand-off exists and codex-exec.sh was used in text mode); Claude did not author the markup inline",
    "Rendered the surface at 1280x800 and produced translate/diff/<state>-r1/report.json with layoutScore and hotspots",
    "Every change in a patch round is justified by a recorded diff observation (a report hotspot cell or a named region of diff.png noted in notes.md) and the patch prompt lists numbered changes plus a FROZEN list",
    "Reported layoutScore as the mockup comparison and never presented pixelDiff against the mockup PNG as the pass/fail number",
    "state.json lists every screenshot and diff image read under stages.translate.viewed",
    "The final round's report.json shows layoutScore \u2265 0.95 against the mockup (structural fidelity, not just process)",
    "No hex or rgb() colour literal anywhere in the surface HTML (HTML entities such as &#9656; are not colours)"
  ]
}