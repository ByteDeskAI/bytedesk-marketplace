# omnigent-dev

Omnigent cross-repo engineering — operator, architect, runtime harness, web deploy, API/SDK, agent bundles.

Install:

```
/plugin install omnigent-dev@bytedesk
```

Bins forward to the platform checkout via `BYTEDESK_REPO_ROOT` / `CLAUDE_PROJECT_DIR`. `scripts/dev/workflow.mjs` remains in the platform repo (ADR-0058).
