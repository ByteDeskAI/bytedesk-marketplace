{
  "name": "fleet-web",
  "version": "1.12.0",
  "private": true,
  "type": "module",
  "description": "fleet plugin web dashboard SPA (Preact + esbuild). Bundle is committed to ../server/dist/ and embedded into the Go binary at build time.",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "typecheck": "tsc --noEmit",
    "dev": "concurrently -k -n esbuild,go -c cyan,magenta 'npm run watch' 'cd server && air -c .air.toml'"
  },
  "dependencies": {
    "@xterm/addon-fit": "^0.10.0",
    "@xterm/xterm": "^5.5.0",
    "preact": "^10.22.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "esbuild": "^0.21.5",
    "typescript": "^5.4.5"
  }
}
