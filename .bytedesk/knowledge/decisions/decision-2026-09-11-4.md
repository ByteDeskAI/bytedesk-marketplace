---
type: Decision
title: Decision 2026-09-11
description: Agent-captured decision
tags:
  - decision
  - captured
status: stable
generated:
  by: knowledge-management/0.1.0
  at: 2026-09-11T21:35:26.788Z
---

# Decision 2026-09-11

Captured from AskUserQuestion.

```json
{
  "session_id": "c3738e82-1fbf-4fc3-a6a3-06f965eac51c",
  "transcript_path": "/home/ryan/.claude/projects/-home-ryan-Documents-GitHub-ByteDeskAI-bytedesk-marketplace/c3738e82-1fbf-4fc3-a6a3-06f965eac51c.jsonl",
  "cwd": "/home/ryan/Documents/GitHub/ByteDeskAI/bytedesk-marketplace",
  "scratchpad_dir": "/tmp/claude-1000/-home-ryan-Documents-GitHub-ByteDeskAI-bytedesk-marketplace/c3738e82-1fbf-4fc3-a6a3-06f965eac51c/scratchpad",
  "prompt_id": "61e3660f-ce72-4613-8d72-3a738c784176",
  "permission_mode": "bypassPermissions",
  "effort": {
    "level": "high"
  },
  "hook_event_name": "PostToolUse",
  "tool_name": "AskUserQuestion",
  "tool_input": {
    "questions": [
      {
        "question": "TM-178 (pool on by default) passes every test. Once merged, 24 other task-management boards on this machine start a pool at their next session, because none sets dispatch.enabled; only this repo is set to off. Workers are `claude` with permissions skipped, and the TM-177 guard blocks force-push, merge, deploy and similar commands. The PR finish line (TM-180) isn't built yet, so until it lands a worker's result stays committed in a worktree. When should I merge?",
        "header": "Merge timing",
        "options": [
          {
            "label": "Hold until TM-180 (Recommended)",
            "description": "Keep TM-178 on its branch. Build TM-179 (dashboard and `tm why`) and TM-180 (PR finish line and docs) on top of it, then merge all three together. The pool is never on by default without the PR step and updated docs."
          },
          {
            "label": "Merge TM-178 now",
            "description": "Merge now. Pools start in the 24 boards at their next session and pick up tasks as they become ready. Until TM-180 lands, a worker's result stays committed in a worktree with no PR opened."
          },
          {
            "label": "Merge now, default off",
            "description": "Merge TM-178, but keep the default off until TM-180 lands, then switch it on. The code is on main sooner, at the cost of one extra change to flip later."
          }
        ],
        "multiSelect": false
      },
      {
        "question": "A waiting (standby) pool process uses about 60 MB of memory. With the pool on, every open session in a repo with a board keeps one, so a second session can take over when the first closes. Keep that?",
        "header": "Standby cost",
        "options": [
          {
            "label": "Keep standby (Recommended)",
            "description": "About 60 MB per extra session per repo. The pool survives the session that started it, as long as any session in that repo stays open."
          },
          {
            "label": "No standby",
            "description": "Extra sessions exit at once, so there's one pool process per repo. When the session running the pool closes, no pool runs until the next session starts or someone runs `tm pool start`."
          }
        ],
        "multiSelect": false
      }
    ],
    "answers": {
      "TM-178 (pool on by default) passes every test. Once merged, 24 other task-management boards on this machine start a pool at their next session, because none sets dispatch.enabled; only this repo is set to off. Workers are `claude` with permissions skipped, and the TM-177 guard blocks force-push, merge, deploy and similar commands. The PR finish line (TM-180) isn't built yet, so until it lands a worker's result stays committed in a worktree. When should I merge?": "Hold until TM-180 (Recommended)",
      "A waiting (standby) pool process uses about 60 MB of memory. With the pool on, every open session in a repo with a board keeps one, so a second session can take over when the first closes. Keep that?": "How can we fix this so a pool runs out-of-band from any session but keep a pool per repo"
    },
    "annotations": {}
  },
  "tool_response": {
    "questions": [
      {
        "question": "TM-178 (pool on by default) passes every test. Once merged, 24 other task-m
```
