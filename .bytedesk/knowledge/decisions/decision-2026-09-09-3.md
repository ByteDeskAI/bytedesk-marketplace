---
type: Decision
title: Decision 2026-09-09
description: Agent-captured decision
tags:
  - decision
  - captured
status: stable
generated:
  by: knowledge-management/0.1.0
  at: 2026-09-09T21:17:13.419Z
---

# Decision 2026-09-09

Captured from AskUserQuestion.

```json
{
  "session_id": "e01dd923-50ea-45d8-9911-b9d5faed94bd",
  "transcript_path": "/home/ryan/.claude/projects/-home-ryan-Documents-GitHub-ByteDeskAI-bytedesk-marketplace/e01dd923-50ea-45d8-9911-b9d5faed94bd.jsonl",
  "cwd": "/home/ryan/Documents/GitHub/ByteDeskAI/bytedesk-marketplace/.bytedesk/worktrees/TM-127-agent-orchestration-persistent-repository-leads-/agent-orchestration",
  "scratchpad_dir": "/tmp/claude-1000/-home-ryan-Documents-GitHub-ByteDeskAI-bytedesk-marketplace/e01dd923-50ea-45d8-9911-b9d5faed94bd/scratchpad",
  "prompt_id": "cb3fc07b-c496-4e42-bf52-67a7a3783e73",
  "permission_mode": "plan",
  "effort": {
    "level": "high"
  },
  "hook_event_name": "PostToolUse",
  "tool_name": "AskUserQuestion",
  "tool_input": {
    "questions": [
      {
        "question": "The gateway terminal header is blocked on a decision that has been sitting since this morning. `GATEWAY-SDK-PROPOSAL.md` asks you to approve a generic `terminal-presentation` SDK contribution — the gateway's own AGENTS.md:80-95 forbids changing the SDK or working around it with a local type without asking you first, so nobody has proceeded. Without it there is no sanctioned way to put role badges and group labels in the header.",
        "header": "SDK contract",
        "options": [
          {
            "label": "Approve the generic contract",
            "description": "A source-neutral `terminal-presentation` contribution: owner-scoped registration, host-authorized terminal IDs only, bounded response (terminalId, freshness, groupPath, badges, ordering), host validates and namespaces, withdrawal removes only that owner's chrome and never closes a terminal. Unblocks the header; costs an SDK release chain plus host resolver/renderer."
          },
          {
            "label": "Approve, but prototype behind a flag first",
            "description": "Same contract, but the gateway ships it disabled by default so the header can be proven end-to-end against real presence data before the SDK release chain is committed to."
          },
          {
            "label": "Defer the header",
            "description": "Do the marketplace-side work only — roles, slots, liveness, delivery guarantee, presence producer — and leave the gateway header for a later cycle once the SDK direction is settled."
          }
        ],
        "multiSelect": false
      },
      {
        "question": "Landing TM-127 (Phase 0) is a review-fix-verify-merge job on 69 files, and the five new primitives are largely independent of each other. How should the subagent fan-out be shaped?",
        "header": "Fan-out",
        "options": [
          {
            "label": "Sequential Phase 0, then parallel (Recommended)",
            "description": "One agent lands TM-127 to main first (it touches every file the others need). Then 4-5 agents work in parallel, each in its own worktree on its own branch, each owning disjoint files. I integrate. Safest ordering, one serialization point."
          },
          {
            "label": "Fully parallel from now",
            "description": "Fan out immediately, with the TM-127 lander and the feature agents working simultaneously. Fastest wall-clock, but every feature agent rebases onto a moving TM-127 and conflicts in topology/lib are near-certain."
          },
          {
            "label": "One agent per phase, sequential",
            "description": "Strictly one agent at a time, each building on the last merged state. Simplest to reason about and review, much slower."
          }
        ],
        "multiSelect": false
      }
    ],
    "answers": {
      "The gateway terminal header is blocked on a decision that has been sitting since this morning. `GATEWAY-SDK-PROPOSAL.md` asks you to approve a generic `terminal-presentation` SDK contribution — the gateway's own AGENTS.md:80-95 forbids changing the SDK or working around it with a local type without asking you first, so nobody has proceeded. Without it there is no sanctioned way
```
