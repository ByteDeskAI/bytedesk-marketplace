{
  "name": "showcase-astra-image-pipeline",
  "description": "A brief in, a finished image out. Codex Astra carries a native image tool (image_gen.imagegen), so the picture is generated by the same agent that reasoned about the brief — no external image skill, no handoff to a separate service. A Claude critic on a different family reviews the render as a picture rather than as a prompt, and Astra revises. The run ends with an actual PNG on disk, which is the point: the deliverable is a file, not a description of a file.",
  "layout": "main-vertical",
  "inputs": {
    "brief": { "description": "What the image is for, and who will see it", "required": true },
    "constraints": { "description": "Palette, medium, format, anything forbidden", "default": "flat vector, no text in the image, works at 64px and at 1024px" },
    "out": { "description": "Where the final image is written, relative to the repo", "default": ".bytedesk/agent-orchestration/artifacts" },
    "rounds": { "description": "Critic-and-revise rounds after the first render", "default": "1" }
  },
  "agents": [
    { "id": "conductor", "role": "orchestrator", "candidates": ["claude:opus"],
      "instructions": "You own the brief and the gate, not the drawing. Send the brief and constraints to the illustrator. When an image path comes back, send the PATH to the critic and ask for a judgement of the rendered picture, not of the prompt. Relay the criticism, run the revision rounds, then stop and report: the final path, its size in bytes, and one sentence on what changed between rounds. Never describe the image yourself — you have not seen it." },
    { "id": "illustrator", "role": "designer", "candidates": ["codex:gpt-6-astra"], "auto_approve": true,
      "instructions": "You are Codex Astra and you have a native image tool, image_gen.imagegen. Read the brief. First write one short paragraph naming the visual direction you have chosen and why it fits the audience — commit to a direction rather than hedging across three. Then generate the image and SAVE IT TO DISK at the path you were given, creating the directory if needed. Reply with the absolute path and the size in bytes, nothing else. On a revision round, edit or regenerate toward the specific criticism and save to a new numbered filename so every round survives for comparison." },
    { "id": "critic", "role": "judge", "candidates": ["claude:opus", "claude:sonnet"],
      "instructions": "You are given a path to a rendered image. Open and look at it. Judge the picture that exists, not the brief it came from: does it read at a glance, does it survive being shrunk, is it one idea or three competing ones, does it break any stated constraint. Name the single most consequential change and say what it would cost. Do not rewrite the brief." }
  ]
}
