---
id: "CAP-0003"
kind: "capability"
status: "open"
created: "2026-09-09T21:55:30.120Z"
board: "bytedeskai/bytedesk-marketplace"
title: "Stop gate cannot tell an abandoned in_progress task from one a live delegated worker owns"
area: "product"
impact: "M"
effort: "M"
confidence: "M"
source: "research"
evidence: []
related: []
updated: "2026-09-10T02:00:26.167Z"
comments: [{"author":"main","ts":"2026-09-10T01:30:45.770Z","text":"LIVE REPRODUCTION, cleaner than the one this was filed from, reported by the TM-135 worktree dispatcher and corroborated by the integrator. Attaching at the dispatcher request; it is not writing the shared board.\n\nThe setup: TM-135 status in_progress (correct), claim held, work genuinely in flight — child agent dispatch2 writing in .bytedesk/worktrees/TM-135-dispatch. Nothing about the task is stale or wrong. And the stop gate still fires, on a session that is not doing anything wrong either.\n\nMechanism, from lib/enforce.mjs gateStopLocked: a dispatched task is exempted only when claims[id].session equals the stopping session. The TM-135 claim names the INTEGRATOR session (2ee26155) because the integrator reclaimed the task from a wrongly-parked state. The dispatcher session therefore gets no exemption and TM-135 is swept into its stop gate on every stop. The gate is not asking for a state change. It is asking that session to prove it holds a claim it does not hold and should not hold.\n\nWhy this reproduction is better than the original: there the record was arguably wrong somewhere. Here the claim is correct, the status is correct, the work is real, the worker is live — and the gate is still wrong. That isolates the defect to the exemption rule itself rather than to bad state.\n\nTWO FACTS THE DISPATCHER DID NOT HAVE:\n\n1. The claiming pid is DEAD. The claim reads pid 423124, ts 2026-09-10T01:20:44Z, and that process is not running. So the exemption is keyed on a session whose process has already exited, while the session actually doing the work is refused the exemption. Any fix that keys on liveness rather than identity has to decide what a dead pid on a live session id means.\n\n2. The store currently holds TWO DISAGREEING RECORDS for TM-135, and that is the integrator error. Reclaiming the task with `tm start` stamped session, actor, branch and worktree with the integrator session and the shared checkout (that is TM-148). The integrator corrected those four fields in the task markdown by hand but did NOT correct the claim in state.json, which still names session 2eede..2ee26155, branch main, worktree the shared checkout. So the task file says the worker owns it and the claim says the integrator does. Half a correction is worse than none here, because it makes the disagreement invisible to anyone reading only one of the two.\n\nNote state.json is machine-local and gitignored, so this half of the disagreement does not travel to a clone — which makes it harder to notice, not easier.\n\nOPTIONS, as the dispatcher ranked them, with the integrator position:\n\n1. Do nothing deliberately, accept that a dispatcher whose task is claimed by the integrator trips its own gate, and let Ryan rule. THE DISPATCHER RECOMMENDS THIS AND THE INTEGRATOR AGREES. The gate firing is accurate: from its point of view that session has unattributed in-flight work.\n2. Treat a task whose claim names ANY live session as attributed, exempting it from every session sweep rather than only the claim-holder. Small change in gateStopLocked; fixes the general case. Worth pricing against fact 1 above — \"live\" needs defining when the pid is dead and the session id is not.\n3. Add the dispatcher session to the claim. Rejected by both. It fabricates co-ownership to quiet a hook, and lib/dispatch collect reconciles worker liveness from that field, so a fabricated value could make collection misjudge a walked-away worker. Same reasoning an earlier coordinator used in refusing to hand-stamp a session id into a null-session claim.\n\nThe integrator has deliberately NOT written the claim. Correcting it means either naming a session whose liveness it has not verified, or releasing a claim on work that is genuinely in flight — and it cannot tell which is safe without reading the gate and collect paths, which is not its role. Recorded rather than silently repaired."},{"author":"main","ts":"2026-09-10T02:00:26.164Z","text":"THE PID FIELD IS USELESS AS A LIVENESS SIGNAL, BY CONSTRUCTION. This kills option 2 in its current form, so it is worth knowing before anyone implements it.\n\nclaims.mjs:68 writes `pid: process.pid`. That is the pid of the SHORT-LIVED `tm` CLI PROCESS that ran the claim verb, not of the agent session doing the work. The CLI exits milliseconds later, so the recorded pid is dead almost immediately and stays dead for the entire life of the claim.\n\nVerified on both live claims in the store rather than inferred from the code alone:\n\n- TM-135, pid 423124, claimed 01:20:44Z — not running.\n- TM-143, pid 2465310, claimed 01:55:38Z — not running, and that claim is minutes old with a worker actively building on its branch right now.\n\nSo a dead pid tells you nothing at all. It does not mean the session is gone; it means a CLI process exited, which it always does. Any \"is this claim live\" test keyed on the pid field answers \"dead\" for every claim in the store, including every healthy one, and would exempt nothing — or, read the other way round, would treat every claim as abandoned and reclaimable, which is worse.\n\nThis corrects the earlier note on this entry, which flagged TM-135 pid 423124 as dead and asked what a dead pid on a live session id means. The answer is: nothing. It is not a signal that needs interpreting, it is a field recording the wrong process.\n\nCONSEQUENCE FOR OPTION 2. \"Treat a task whose claim names any LIVE session as attributed\" needs a liveness source that actually exists. The pid is not it. Candidates worth pricing, none verified here: the harness session registry the orchestration layer already keeps, the tmux binding, or a heartbeat the claim holder refreshes — the last being what `startHeartbeat` already does for dispatch, and therefore the cheapest thing to reuse.\n\nEither the pid field should record the session process rather than the CLI process, or it should be dropped, because a field that is always wrong is worse than an absent one — somebody will eventually trust it."}]
---

Observed repeatedly this session. A lead session running a fan-out has N tasks legitimately in_progress, each owned by a live teammate in its own worktree. done and block are both false, and park would misreport active work as paused — so the only honest action is to arm 'tm override' every turn, which trains the reflex the gate exists to prevent and wears out a mechanism meant for exceptions. The gate is right that an in_progress task with nobody on it is a lie the next session inherits; it just has no way to see the difference. Note this is adjacent to CAP-0002: both come from the store having no reliable notion of WHO is on a task right now. Possible shapes, cheapest first: (a) treat a task whose claim names a live session or agent as attributed, and exempt it from the stop sweep the way a dispatched task already is; (b) let a task record a delegated owner explicitly ('tm delegate <id> <agent>') so the gate can check liveness against the agent registry, which already tracks alive/dead; (c) have the gate report rather than block when every in_progress task has a live owner. Whatever the shape, the goal is that a lead running a fan-out never has to reach for override, and an genuinely abandoned task still gets caught.