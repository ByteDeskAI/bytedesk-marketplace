---
id: "TM-152"
kind: "task"
status: "open"
created: "2026-09-10T02:01:47.863Z"
board: "bytedeskai/bytedesk-marketplace"
title: "agent-orchestration: build:check is location-dependent — it fails in a linked worktree and passes in the canonical checkout"
epic: "EP-018"
acceptance: [{"text":"The cause is established rather than assumed: either a source commit that changed ACP inputs without rebuilding, or an esbuild/toolchain version change that makes the tracked bytes unreproducible. Say which, with the revision that did it.","done":true,"at":"2026-09-10T02:04:15.952Z"},{"text":"npm run build:check exits zero on main.","done":true,"at":"2026-09-10T02:04:25.500Z"},{"text":"If the cause was a toolchain drift rather than a missed rebuild, the check itself is made deterministic or the gate is documented as toolchain-pinned — a gate nobody can reproduce is not a gate.","done":false},{"text":"build:check produces identical bytes from the canonical checkout and from a linked worktree whose node_modules is a symlink","done":false}]
evidence: []
commits: ["fb1558c","781913f","f840403"]
blockedBy: []
blocks: []
actor: "main"
session: "e01dd923-50ea-45d8-9911-b9d5faed94bd"
branch: "main"
worktree: "/home/ryan/Documents/GitHub/ByteDeskAI/bytedesk-marketplace"
updated: "2026-09-10T02:09:48.247Z"
comments: [{"author":"main","ts":"2026-09-10T02:04:10.161Z","text":"CAUSE ESTABLISHED, and the premise is wrong. build:check does NOT fail on main. It fails in a LINKED WORKTREE, and passes in the canonical checkout, at the same revision with the same toolchain.\n\nMeasured:\n\n  shared checkout /home/ryan/.../bytedesk-marketplace/agent-orchestration   npm run build:check  EXIT 0\n  worktree .bytedesk/worktrees/TM-143-refusal/agent-orchestration           npm run build:check  EXIT 1\n  esbuild in both                                                            0.28.2 — identical\n\nSo it is neither a missed rebuild nor toolchain drift. It is the build directory.\n\nWHY. The worktree`s node_modules is a SYMLINK:\n\n  node_modules -> ../../../../agent-orchestration/node_modules\n\nesbuild writes each bundled module`s resolved path into the output as a comment. From the canonical checkout it resolves `node_modules/@anthropic-ai/claude-agent-sdk/sdk.mjs`; through the symlink it resolves `../../../../agent-orchestration/node_modules/@anthropic-ai/claude-agent-sdk/sdk.mjs`. The first byte that differs is 310, in exactly that comment. 97 module comments are rewritten, and the rebuilt bundle is 2,323,814 bytes against the tracked 2,320,710 — 3,104 bytes of extra path text and nothing else. Same 97 modules either side; only the spelling of their paths differs.\n\nThe tracked bytes are therefore reproducible and current. dist/claude-agent-acp.mjs is NOT stale.\n\nTHE CONTROL IS WHAT CAUSED IT. The report says a `git archive main` extract fails identically, using the SAME node_modules as the working tree deliberately, so that esbuild is held constant. Sharing node_modules is exactly the confounder: it can only be shared by symlink or by pointing outside the tree, and that is precisely what rewrites the paths. Holding esbuild constant that way varies the thing the check actually measures.\n\nThis is the third instance of one shape in this epic, and the sharpest: a verification run in an isolated tree where the isolation itself is the confounder. First a symlinked node_modules shared between tree and control. Then two git archive trees with no .git, which made the MCP handshake test fail by construction (TM-149). Now a symlinked node_modules again, rewriting embedded paths. Each control was chosen carefully and each shared the confounder with the thing it was controlling.\n\nWHAT IS ACTUALLY WRONG, AND WORTH FIXING. build:check is not location-independent. It only passes from one directory on this machine, so every worktree-based worker sees a false failure and has no way to tell it from a real one — as happened here. A gate that reports failure based on where it was run is not a usable gate for anyone working in a worktree, which is how every dispatched worker in this epic works.\n\nRetargeting this task accordingly: the bundle does not need rebuilding, the check needs to stop embedding checkout-relative paths. esbuild `absWorkingDir`, or resolving node_modules through its realpath before building, are the obvious candidates; neither is verified here.\n\nFOR EVERY EP-018 TASK THAT REPORTED build:check PASS: those readings stand. The failure was never on main. But the reverse warning is now live and matters more — a worker reporting build:check FAILURE from a worktree is reporting the worktree, not the code.\n\nOne correction to my own record while I am here. In the TM-135 merge gate I reported build:check as passing having read only the tail of its output, without checking the exit code. The result was right and the method was not; I have since re-run it capturing the exit status, which is how the discrepancy above was resolved."}]
---

`npm run build:check` (scripts/check-bundle.mjs) exits non-zero on unmodified main with "Tracked bundle is stale: dist/claude-agent-acp.mjs". Reproduced with a genuine control: a `git archive main` extract, using the SAME node_modules as the working tree so esbuild is held constant, fails identically. So it is not caused by any branch in flight — TM-143's diff touches topology/lib/mailbox.mjs and no ACP source at all, and fails the same way.

This matters because build:check is a gate every EP-018 task has been reporting as passing. It ran green during TM-135 (base 36b9308) and fails at main 9c765c3, so the staleness entered somewhere between those two revisions, or the bundle was always stale and an earlier run compared it against a different esbuild.

The tracked bundle was last rebuilt at 9e30405 (agent-orchestration 0.2.3), which is old. The likely fix is simply `npm run build` and committing the regenerated dist, but the cause must be established first: a bundle that silently drifts from its sources is worse than a stale one, because check-bundle.mjs is the only thing that would tell anyone.