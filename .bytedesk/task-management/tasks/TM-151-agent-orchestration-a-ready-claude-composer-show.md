---
id: "TM-151"
kind: "task"
status: "blocked"
created: "2026-09-10T01:35:36.878Z"
board: "bytedeskai/bytedesk-marketplace"
title: "agent-orchestration: a ready Claude composer showing a placeholder hint reads as not-ready, in both claude.json ready patterns"
acceptance: [{"text":"A ready pane showing Claude's placeholder hint is detected as ready, in both ready.pattern and ready.tmux_pattern","done":false},{"text":"A pane that is genuinely mid-answer is still not detected as ready","done":true,"at":"2026-09-10T02:29:40.895Z"},{"text":"The measurement is redone against a live pane in both states, not reasoned about","done":true,"at":"2026-09-10T02:29:41.052Z"},{"text":"All four claude.json fields are decided deliberately and per-field — ready.tmux_pattern, ready.pattern, composer.empty_tmux_pattern, composer.empty_pattern — with the composer TM-111 negative preserved and the choice argued, not inherited by find-and-replace","done":true,"at":"2026-09-10T02:29:41.196Z"}]
evidence: [".bytedesk/task-management/evidence/TM-151-TM-151-PARTIAL-FIX.md"]
commits: ["94b48a1","9dd855f","ff628cb","44a4d26","dbbbb99","1910b95"]
blockedBy: []
blocks: []
actor: "main"
session: "e01dd923-50ea-45d8-9911-b9d5faed94bd"
branch: "main"
worktree: "/home/ryan/Documents/GitHub/ByteDeskAI/bytedesk-marketplace"
updated: "2026-09-10T03:13:21.206Z"
type: "bug"
labels: ["plugin:agent-orchestration"]
priority: "highest"
comments: [{"author":"main","ts":"2026-09-10T01:38:05.803Z","text":"IMPLEMENTATION SHAPE, proposed by the TM-135 dispatcher and tested by the integrator. It works. But testing it surfaced that this task is bigger than two fields, and that a blanket fix is dangerous.\n\nTHE PROPOSED SHAPE. Do not reason about \"emptiness\". A placeholder is a fixed known string and real content is not, so the pattern becomes: bare prompt glyph OR prompt glyph followed by this provider known placeholder prefix. For claude that prefix is `Try \"` — the hint always begins that way and only the tail varies. A user or an agent mid-answer does not begin a line that way. It degrades safely: an unseen placeholder reads as not-ready, which is today behaviour, not a regression.\n\nTested candidate: `^\\\\s*[│|]?\\\\s*[>❯]([^a-zA-Z0-9]*$|[^a-zA-Z0-9]*Try \")`\n\n| line | matches |\n|---|---|\n| bare composer `❯ ` | YES |\n| placeholder `❯ Try \"write a test for <filepath>\"` | YES |\n| agent mid-answer `❯ read the inbox file 001-ping` | no |\n| TM-111 plan-approval modal `❯ 1. Yes, and switch to BYPASS PERMISSIONS` | no |\n\nIt also survives assertTmuxPattern: no `{`, `}` or `:`, no newline, no trailing whitespace class.\n\nFOUR FIELDS, NOT TWO. The same two strings are declared in four places in claude.json:\n\n  ready.tmux_pattern           ^\\\\s*[│|]?\\\\s*[>❯][^a-zA-Z0-9]*$\n  composer.empty_tmux_pattern  ^\\\\s*[│|]?\\\\s*[>❯][^a-zA-Z0-9]*$          (identical)\n  ready.pattern                (^|\\\\n)\\\\s*[│|]?\\\\s*[>❯][^a-zA-Z0-9\\\\n]*$\n  composer.empty_pattern       (^|\\\\n)\\\\s*[│|]?\\\\s*[>❯][^a-zA-Z0-9\\\\n]*$  (identical)\n\nDO NOT BLANKET-REPLACE THEM. The composer note says exactly why, and it is load-bearing safety rather than tidiness: the negative is what makes it a COMPOSER test rather than a prompt test. It answers 0 on `❯ read the inbox file 001-ping`, and it answers 0 on the plan-approval modal `❯ 1. Yes, and switch to BYPASS PERMISSIONS` — the TM-111 case where pressing Enter means something destructive. The note also states the two are declared separately BECAUSE codex proves they are not the same question, and a find-and-replace across all four re-conflates precisely what that separation exists to keep apart.\n\nSo: decide per field. For ready.* admitting the placeholder is the fix. For composer.empty_* admitting it is arguably also correct — a composer showing a hint IS empty — but that is a second judgment with TM-111 consequences and must be argued separately, not inherited.\n\nNOTE THE CODEX PRECEDENT RUNS THE OTHER WAY. TM-130 fixed codex by matching ONLY the literal placeholder: `^\\\\s*›\\\\s*Ask Codex to do anything`, no bare-glyph branch at all. So the two adapters would end up with structurally different ready patterns — codex requires the hint, claude admits it optionally. That is defensible, since the CLIs render differently, but whoever implements should state which they chose and why rather than making them look alike.\n\nTWO CONSTRAINTS TO HOLD THE IMPLEMENTER TO, both of which already bit this epic. The tmux form must pass assertTmuxPattern while the JS form has no such limit, so the two will not be textually identical even though they must mean the same thing — that asymmetry is a trap, not an inconsistency to tidy away. And it must be MEASURED against a live pane in all three states — bare, placeholder, mid-answer — with the numbers recorded in the adapter note the way TM-130 did and the way the existing composer note does. Both of these patterns shipped after being verified by reading. That is how they got here."},{"author":"main","ts":"2026-09-10T02:21:45.088Z","text":"REPRODUCED LIVE, with measurements, during the EP-018 demo run in bytedesk-tmux-manager (isolated socket, torn down after).\n\nA reviewer session came up READY — Claude Code v2.1.267, safe mode banner, composer rendering the placeholder hint:\n\n    ❯ Try \"fix lint errors\"\n\nMeasured with tmux's own matcher against that exact live pane, and against a second one showing `❯ Try \"create a util logging.py that...\"`. #{C/r:} returns the line number of the first match, or 0:\n\n    shipped   ready.tmux_pattern   ^\\s*[│|]?\\s*[>❯][^a-zA-Z0-9]*$                            -> 0   (both panes)\n    candidate                      ^\\s*[│|]?\\s*[>❯]([^a-zA-Z0-9]*$|[^a-zA-Z0-9]*Try \")      -> 12  (both panes)\n\nCONSEQUENCE, and it is larger than a slow launch. Readiness failing means the role session is never REGISTERED: role status reviewer read state none, registered false, alive false after the session was up and idle. A governed launch requires a responsive lead AND reviewer, so TOPOLOGY_STARTUP_NOT_READY refuses every orchestration. On this machine, with this pattern, no governed run can start at all.\n\nThe census disagreed with the readiness check about the same pane and the census was right: it reported the reviewer as `idle` (\"no spinner and nothing waiting on a human\") while the launcher called it not-ready. Two subsystems, two answers, one pane.\n\nThe JS sibling needs its own shape, not a copy of the tmux one — the shipped ready.pattern is (^|\\n)-anchored with [^a-zA-Z0-9\\n], so the fix is:\n\n    tmux: ^\\s*[│|]?\\s*[>❯]([^a-zA-Z0-9]*$|[^a-zA-Z0-9]*Try \")\n    js:   (^|\\n)\\s*[│|]?\\s*[>❯]([^a-zA-Z0-9\\n]*$|[^a-zA-Z0-9\\n]*Try \")\n\nChecked against the negative that matters: '❯ half typed text' does NOT match either form, so a composer holding an unsent draft still reads as non-empty. All four fields need it — ready.tmux_pattern, ready.pattern, composer.empty_tmux_pattern, composer.empty_pattern — because the composer pair is byte-identical to the ready pair and a placeholder hint means the composer IS empty.\n\nALSO OBSERVED, worth its own line: each failed `reviewer ensure` spawned a NEW session and left the previous one alive. Four attempts left four abandoned reviewer sessions plus the lead. 'Session preserved' is right for one attempt; across retries it accumulates, and nothing reaps them."},{"author":"main","ts":"2026-09-10T02:23:16.077Z","text":"ESCALATED TO HIGHEST. THIS BLOCKS EVERY GOVERNED LAUNCH ON THIS MACHINE. Measured on live panes by the worktree dispatcher during two real orchestration runs; report at .bytedesk/task-management/evidence/EP-018-DEMO-2026-09-09.md.\n\nThe reviewer came up HEALTHY, composer rendering `❯ Try \"fix lint errors\"`. Using tmux own matcher, which returns the line number of the first match or 0:\n\n  shipped ready.tmux_pattern                            -> 0   on both panes\n  candidate with the placeholder alternation            -> 12  on both panes\n\nThe consequence is bigger than a slow start. Readiness failing means the session is never REGISTERED: `role status reviewer` read `state: none, registered: false, alive: false` while the session sat there idle and working. A governed launch requires a responsive lead AND reviewer, so TOPOLOGY_STARTUP_NOT_READY refuses everything. Nothing governed can start until this regex is fixed.\n\nBOTH FORMS VERIFIED BY THE INTEGRATOR, positives and negatives:\n\n  tmux: ^\\\\s*[│|]?\\\\s*[>❯]([^a-zA-Z0-9]*$|[^a-zA-Z0-9]*Try \")\n  js:   (^|\\\\n)\\\\s*[│|]?\\\\s*[>❯]([^a-zA-Z0-9\\\\n]*$|[^a-zA-Z0-9\\\\n]*Try \")\n\n  MATCH   bare composer, bare boxed composer, `❯ Try \"fix lint errors\"`, `❯ Try \"how does server.go work?\"`\n  REJECT  `❯ half typed text` — an unsent draft still reads as non-empty\n  REJECT  `❯ read the inbox file 001-ping` — agent mid-answer\n  REJECT  `❯ 1. Yes, and switch to BYPASS PERMISSIONS` — the TM-111 modal where Enter is destructive\n  REJECT  `❯ 1. Yes, I trust this folder` — the folder-trust modal\n\nThe tmux form passes assertTmuxPattern: no braces or colon, no newline, no trailing whitespace class. The JS form needs its own shape rather than a copy, because the shipped sibling is (^|\\\\n)-anchored and uses [^a-zA-Z0-9\\\\n] — which is exactly the asymmetry that must NOT be tidied away.\n\nThe half-typed-draft negative is the one that matters most and it was the dispatcher own choice of control. It is what stops this fix from becoming \"permit trailing text\", which would make a pane mid-answer read as ready and hand a bootstrap to an agent that is talking.\n\nTWO FURTHER FINDINGS FROM THE RUNS:\n\nTHE CENSUS AND THE READINESS CHECK DISAGREED ABOUT THE SAME PANE, AND THE CENSUS WAS RIGHT. It called the reviewer `idle` — no spinner, nothing waiting on a human — while the launcher called it not-ready. The census has now been correct on both occasions where the two differed. Worth weighing when this fix is gated: if the two ever disagree again, the census reading is the one with the better record, and there is a design question underneath about why readiness re-derives something the census already knows.\n\nEACH FAILED `reviewer ensure` SPAWNS A NEW SESSION AND LEAVES THE PREVIOUS ONE ALIVE. Four attempts left four abandoned reviewers plus the lead. \"Session preserved\" is correct for a single failure — it keeps the evidence — but nothing reaps them across retries. Recorded here rather than filed separately because the retry loop only exists because of this defect; if TM-151 is fixed the leak stops being reachable by this route. It should still be checked as part of the fix, not assumed gone."},{"author":"main","ts":"2026-09-10T02:26:48.338Z","text":"MY OWN FIX IS INSUFFICIENT, and the same demo that proved the bug proved that too. Reporting before it ships rather than after.\n\nThe placeholder hint is not the only thing a ready Claude composer renders. Measured on the two live role panes just now, after both agents had bootstrapped and gone idle:\n\n    pane %0 composer:  ❯ init the task store\n    pane %1 composer:  ❯ Read /home/…/agents/b072692f/prompt.md and begin your standing role. Poll\n                          your protocol inbox at safe boundaries.\n\n    shipped pattern            -> 0 on both\n    my TM-151 candidate        -> 0 on both\n\nSo the `Try \"` branch fixes exactly the fresh-session hint and nothing else. A ready pane can carry a suggestion Claude generated from its own last turn, and pane %1 is carrying the BOOTSTRAP POINTER ITSELF, typed and never submitted — which is TM-130's `typed-unsubmitted` state sitting in front of us, on the very run that was trying to prove readiness.\n\nThat last one matters for the shape of the fix: widening the pattern to admit arbitrary text after the glyph would make an unsent draft read as READY and as EMPTY, and the delivery ladder would type a second copy on top of it. The negative I was proud of — '❯ half typed text' must not match — is the thing that makes a text-pattern approach unable to answer this question at all. A composer holding a suggestion and a composer holding a draft render identically in a capture; the difference is styling, and a capture has none.\n\nSo the honest conclusion is that ready/composer-empty cannot be decided from the composer LINE alone for Claude. The candidates I can see, none free:\n  - ask tmux for the styled cell rather than the text — dim/ghost rendering is the actual discriminator, and  keeps escape sequences;\n  - use the status line instead, which reads '⏵⏵ auto mode on … · N tokens' when idle and shows a spinner otherwise — the census already reads exactly this and has been right about these panes three times running;\n  - keep the pattern for the empty case and treat 'not empty' as unknown rather than not-ready, so the launcher stops refusing on it.\n\nI have left my four-field change in place on tm/TM-151-ready with its tests, because it is strictly better than shipped — it fixes the fresh-session case that blocks every FIRST launch — but AC1 is not met by it and I am not claiming it is. The task needs the design decision above, and TM-157 is the other half of why nothing governed starts."},{"author":"main","ts":"2026-09-10T02:27:05.502Z","text":"TWO CORRECTIONS TO MY PREVIOUS COMMENT.\n\n1. A shell backtick ate three words in it. The first candidate should read: ask tmux for the STYLED cell rather than the text, because dim ghost rendering is the actual discriminator and \"capture-pane -e\" keeps the escape sequences that carry it.\n\n2. I stated that pane %1 was holding the bootstrap pointer \"typed and never submitted\". What I actually observed is that the composer line CONTAINED that text while the agent had already bootstrapped and gone idle. Two readings fit: a delivery that typed and failed to submit (TM-130 typed-unsubmitted), or Claude re-rendering the last queued input after the turn ended. I did not distinguish them, and the difference changes what the fix must do, so I am not claiming the first. What is certain, and is enough for this task: the composer line carried arbitrary non-hint text on a pane that was ready, and both the shipped pattern and my candidate answered 0 on it."},{"author":"main","ts":"2026-09-10T02:33:01.466Z","text":"MERGED at ff628cb — STILL BLOCKED, AC1 UNMET. The author declined to tick it and that was correct; the integrator is not ticking it either.\n\nGates on the merged tree, exit codes captured: topology 335/335, unit 510 tests / 506 pass / 4 skipped, build:check, roadmap:check, both frozen validators unmodified. Plus the five src suites — mcp-contract, runtime-engine, service-routing, session-host, session-supervisor — 41 of 41, run HERE because the author would not run them in a worktree with no node_modules and refused to symlink one in, that being exactly what produced TM-152 false failure. A worker declining to manufacture a green is worth more than the green.\n\nWHAT THIS FIXES: the fresh-session hint only. `❯ Try \"fix lint errors\"` now reads ready, all four fields, TM-111 negative preserved and tested.\n\nWHAT IT DOES NOT FIX, and the reason is structural rather than a missing case. After an agent goes idle its composer reads `❯ init the task store` or `❯ Read /home/…/prompt.md and begin your standing role…`. Shipped scores 0; this fix also scores 0. The obvious extension — admit trailing text — is WRONG, and the half-typed-draft negative is exactly why: an unsent draft would then read as ready AND empty, so the delivery ladder would type a second copy over it.\n\nThe deeper point the author made and it is worth keeping: a suggestion and a draft are IDENTICAL in a text capture. The difference is styling. So no text pattern over a plain capture can answer this question, and the negative that makes this fix safe is the same property that makes the general case unanswerable this way.\n\nThree directions on the task, none verified: read the styled cell (capture-pane -e keeps the escapes); use the status line the census already reads; or treat \"not empty\" as UNKNOWN rather than not-ready. The third is closest to the standing observation that readiness re-derives what the census already knows — and the census has now been right FOUR times out of four where the two disagreed. At four-for-four that stops being an anecdote and becomes an argument about which layer should own the question.\n\nMERGE RATIONALE: strictly better than shipped, negatives verified, no regression. Not merged as a completion. TM-157 means governed launch is blocked regardless of this — the reviewer can never be responsive, by construction, for two independent reasons."},{"author":"main","ts":"2026-09-10T02:38:12.077Z","text":"THE DISCRIMINATOR EXISTS AND I HAVE IT MEASURED. This answers the open design question in my earlier comment, and it is not a regex.\n\nCaptured with escape sequences kept, on the live lead pane, two composer lines from the same session:\n\n    \\033[38;5;239m\\033[48;5;237m❯ \\033[38;5;231mRead /home/…/agents/cef24b50/prompt.md and begin\n    your standing role. Poll your protocol inbox at safe boundaries.\\033[39m\n\n    \\033[39m❯ \\302\\240\\033[2mrun tm init\\033[0m\n\nThe first is REAL TYPED TEXT: the content after the glyph is 38;5;231, bright white. The second is a SUGGESTION: the content after the glyph is wrapped in SGR 2 — dim — and closed with 0. Claude renders ghost text dim and typed text bright, and tmux keeps that distinction in the capture.\n\nSo the question \"is this composer empty\" IS answerable, just not from the plain text. A composer whose post-glyph content is entirely dim is EMPTY: what is on the line is a suggestion the user has not accepted. A composer carrying bright text after the glyph holds a draft and must never be typed into.\n\nCONSEQUENCES, and they are larger than this task:\n\n1. The plain-text pattern cannot be made correct by widening, and I have now proved that rather than argued it. A suggestion and a draft are identical in `capture-pane -p` and different in `capture-pane -pe`. Every version of this regex — shipped, mine, and any future one — is guessing at a distinction that is one flag away.\n\n2. tmux `#{C/r:}` matches plain text server-side, so the styled test cannot be a format. It has to be a capture, which costs one call per pane. That is the real trade: the cheap server-side test stays as the fast path and answers \"empty\" correctly for a bare composer; the styled capture is taken only when the cheap test says NOT empty, which is exactly when we would otherwise refuse to act.\n\n3. It explains every symptom in this epic that pointed at the composer. TM-151 fresh-session hints are dim. Suggestions after a turn are dim. A stuck draft is bright. The delivery state machine wants precisely this: `typed-unsubmitted` is bright text, `submitted` is a composer whose only content is dim.\n\nI have NOT implemented it. It touches the delivery layer that everything else rests on, and the integrator is right that the choice of which layer owns readiness is Ryan sanction rather than a worktree decision. But the measurement removes the uncertainty from the decision: this is not \"pick a better heuristic\", it is \"the information is available and we are not reading it\"."}]
evidenceSources: {".bytedesk/task-management/evidence/TM-151-TM-151-PARTIAL-FIX.md":{"source":"/home/ryan/Documents/GitHub/ByteDeskAI/bytedesk-marketplace/.bytedesk/worktrees/TM-151-ready/.bytedesk/task-management/evidence/TM-151-PARTIAL-FIX.md","sha256":"cb0ab27a0ea1a40c1df5be3005e8c804f5b410ffbe452ab12fe7237e79dae300","bytes":4490,"at":"2026-09-10T02:29:41.327Z"}}
blockedReason: "PARTIAL FIX READY, TASK NOT DONE. Branch tm/TM-151-ready-placeholder off main@a31042c, code commit 94b48a1, evidence 8e8a0b1. AC2, AC3 and AC4 are met: the mid-answer and both TM-111 modals still read not-ready, the measurement was redone on live panes (shipped 0, fixed 12 on two ready reviewer panes), and all four fields were decided per field with the argument in each note. AC1 is NOT met and I am not claiming it: after the agents bootstrapped and went idle their composers read 'init the task store' and the bootstrap pointer text, and both the shipped pattern and this fix answer 0 on those. A ready pane can carry arbitrary text, and admitting trailing text would make an unsent draft read as ready AND empty so the ladder would type over it — a suggestion and a draft are identical in a capture because the difference is styling. The remaining decision is on the task: read the styled cell (capture-pane -e), use the status line the census already reads, or treat 'not empty' as unknown rather than not-ready. Merging this is still strictly better than shipping — it unblocks the FIRST launch in any fresh session. It does not make a governed launch work: TM-157 (the reviewer has no shell under --restricted --safe-mode and so can never acknowledge a nonce) is the other half. Gates: topology 335/335, both frozen presence validators unmodified, roadmap OK; the five src/ suites were not run here for want of node_modules and pass 41/41 in the canonical checkout."
---

`providers/claude.json` decides a Claude pane is ready with two regexes that both end `[^a-zA-Z0-9]*$`. That forbids letters after the prompt glyph. When Claude renders a placeholder hint into an otherwise empty, ready composer — `❯ Try "write a test for <filepath>"` — the letters in the hint make a ready pane read as not-ready, and `role ensure reviewer` fails with `TOPOLOGY_SESSION_START`.

Measured by the TM-135 dispatcher against a live reviewer pane sitting ready at that placeholder: shipped `tmux_pattern` matched 0 lines; `^\s*❯` matched at line 12; `.` matched 1 as a sanity control. Reproduced twice.

BOTH PATTERNS ARE AFFECTED, not just the one measured. The integrator tested each against the live placeholder string:

| pattern | bare composer | placeholder hint |
|---|---|---|
| `ready.tmux_pattern` — `^\s*[│\|]?\s*[>❯][^a-zA-Z0-9]*$` | match | NO MATCH |
| `ready.pattern` — `(^\|\n)\s*[│\|]?\s*[>❯][^a-zA-Z0-9\n]*$` | match | NO MATCH |

So fixing only `tmux_pattern` leaves the non-tmux readiness path broken in the same way.

Same class as the Codex defect TM-130 fixed: Codex had exactly this shape with `› Ask Codex to do anything`, measured 0, and was corrected. Claude was measured at that time against a pane whose composer happened to be bare, so this case was never seen.

Why it looked like it worked: the lead succeeded only because its composer was bare at the moment of its readiness check. That is a race against what Claude chooses to render, not a property of the role — which is why this reproduces on the reviewer and not reliably on the lead.

The fix must not simply allow any trailing text, or a pane mid-answer reads as ready. It needs to distinguish an empty composer showing a hint from a composer with real content, and that distinction should be stated wherever it lands.