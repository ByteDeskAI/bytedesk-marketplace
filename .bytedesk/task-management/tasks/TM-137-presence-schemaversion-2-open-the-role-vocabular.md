---
id: "TM-137"
kind: "task"
status: "done"
created: "2026-09-09T21:32:52.887Z"
board: "bytedeskai/bytedesk-marketplace"
title: "Presence schemaVersion 2: open the role vocabularies for standing designer and image-gen"
epic: "EP-018"
acceptance: [{"text":"The v2 addendum opens repoRole and runRole — preferably by making rendering roleName-driven rather than by enumerating every future role — with its own fixture set and its own countersignature from the gateway coordinator.","done":true,"at":"2026-09-10T23:24:33.485Z"},{"text":"A v2-aware consumer still parses v1 correctly, and the version bump lands on both sides in the same negotiated step.","done":true,"at":"2026-09-10T23:24:33.637Z"}]
evidence: [".bytedesk/task-management/evidence/TM-137-TM137.md",".bytedesk/task-management/evidence/TM-137-PRODUCER-V2.md"]
commits: ["4d5adc2","2185219","b112aef","41a1b93","904be43"]
blockedBy: ["TM-136"]
blocks: []
actor: "main"
session: "e01dd923-50ea-45d8-9911-b9d5faed94bd"
branch: "main"
worktree: "/home/ryan/Documents/GitHub/ByteDeskAI/bytedesk-marketplace"
updated: "2026-09-10T23:29:04.968Z"
comments: [{"author":"main","ts":"2026-09-10T05:55:19.783Z","text":"ASSESSED BY THE INTEGRATOR, NOT STARTED, AND THE REASON IS A DESIGN CONFLICT RATHER THAN CAPACITY.\n\nTM-137 suggests opening the vocabularies \"preferably by making rendering roleName-driven rather than by enumerating every future role\". That approach CONTRADICTS a clause the gateway has just countersigned.\n\nPRESENCE-HEADER-ADDENDUM.md section 9.5, in the consumer obligations the countersignature accepted:\n\n  \"roleName is display metadata. Never group on it, never label Team lead from it.\"\n\nSo roleName was signed specifically as a field a consumer must NOT render roles from. Making rendering roleName-driven is not an incremental step from that position; it reverses the obligation both sides agreed hours ago. That is still a legitimate v2 change — v2 is where the vocabularies open — but it has to be negotiated as a REVERSAL and stated as one in the request, not slipped in as the natural extension it looks like.\n\nSection 10 of the same addendum already scopes this task exactly: opening repoRole to designer or image-gen, opening runRole to image-gen, and it notes each fails the frozen validator by construction, with n01-repo-role-designer.json as the standing proof for the first. That fixture exists and passes today, so the mechanical half of the argument is already made.\n\nWHAT BLOCKS COMPLETION, and it is external, not effort:\n\nAC1 requires \"its own countersignature from the gateway coordinator\". That is the same shape as TM-136 AC7 and cannot be produced from inside this repository.\n\nAC2 requires that \"the version bump lands on both sides in the same negotiated step\". The other side is bytedesk-remote-gateway, a different repository with its own consumer at origin/tm/TM-222-orchestration-terminals-consume-presence-and-gro. A unilateral schemaVersion bump here would break that consumer, and the task body says plainly: do not promise the operator a header that labels a standing designer before this lands.\n\nWHAT IS PREPARABLE WITHOUT THE GATEWAY, and what the integrator has deliberately NOT done yet: a v2 addendum draft, a presence-v2 fixture set, and a countersignature request naming the section 9.5 reversal explicitly. All three are worth doing and none is worth doing until the section 9.5 question is answered, because the answer changes what the spec says. Writing the spec first and negotiating second is how the reversal gets buried.\n\nTHE PRODUCER IS UNTOUCHED AND SHOULD STAY SO. schemaVersion remains 1. TM-138 landed all six additive keys under v1 precisely so that a standing designer is VISIBLE — carried in roleName, mapped to a legal runRole, never dropped — while the labelling question waits for v2. That is the whole reason the drop bug was fixed additively rather than by opening the vocabulary."},{"author":"main","ts":"2026-09-10T21:06:52.929Z","text":"COUNTERSIGNATURE REQUEST WRITTEN, AND THE V2 VALIDATOR WAS PASSING BY CHECKING NOTHING. Branch tm/TM-137-v2-request off main@d97c9de, commit 23096d6. Both criteria stay externally gated — this does not close the task, it makes it signable.\n\nTHE DEFECT FIRST. validate_presence_v2.py had 'for arg in argv:' with no fallback, where the frozen v1 validator has 'or sorted(here.glob(\"*.json\"))'. So running it with no arguments iterated ZERO files and printed 'ok — 0 snapshot(s)', which reads as a pass. The v2 fixtures had never been checked by the v2 validator at all. That is verification-that-can-fail section 1 — a clean result that would look identical if the thing being checked were absent — sitting inside the artifact whose entire job is to prove v2 is safe. Fixed; it now reports 3 for 3 fixtures, and a test asserts the reported count equals the fixtures on disk.\n\nWHAT IS NOW MEASURED, each command exactly as the request prints it:\n  frozen v1 validator, unmodified, over its own corpus   ok — 7 snapshot(s)\n  frozen v1 validator over a v2 snapshot                 2 violation(s)   <- the argument for a bump\n  v2 validator over its own fixtures                     ok — 3 snapshot(s)\n  v2 validator over the whole v1 corpus                  ok — 7 snapshot(s)  <- AC2's testable half\nThe producer still emits schemaVersion 1 and a test guards that.\n\nTHE REQUEST. topology/V2-COUNTERSIGNATURE-REQUEST.md — TM-136 got signed because it shipped one of these; the v2 draft had an addendum and no ask. It states what we want confirmed, why enumeration KEEPS the gateway's own section 9.5 rule rather than reversing it, the three verification commands with expected output, hashes for the v2 artifacts and for the untouched frozen ones, and the promise that nothing here publishes schemaVersion 2 until they reply. It discloses the zero-snapshot defect in the same document rather than quietly fixing it first, because they are being asked to trust those numbers.\n\nWHAT REMAINS AND CANNOT BE DONE HERE: the gateway coordinator's signature, and AC2's other half — the bump landing on both sides in one negotiated step. Neither is producible from this repository, and I am not claiming either."},{"author":"main","ts":"2026-09-10T21:56:00.490Z","text":"Request document made reviewable. Committed 1ce3358 on tm/TM-137-request-evidence, awaiting integrator merge. TM-137 STAYS PARKED — this does not unblock it and nothing in it is authorization.\n\nThe gateway session reviewed the request today, declined to countersign because nobody gave that session the coordinator role, and found a real defect instead. It is worth recording precisely, because the defect was in the artifact rather than in anyone's reading of it.\n\n§2 offered this as the evidence that opening the role vocabulary requires v2:\n\n    FAIL v01-standing-designer.json: k3n8vq2a: repoRole 'designer'\n    2 violation(s)\n\nQuoted TRUNCATED. The real output carries a schemaVersion violation above it, which is why the count is 2. And the truncation is load-bearing: a v2 fixture differs from a passing v1 snapshot in two ways at once, the roles AND the version, and a version-1 validator refuses anything declaring version 2 by definition. So that run can only ever come back red, whatever the vocabulary does. As proof that the ROLES forced the bump it is circular — §4.1 was asking a reviewer to confirm a tautology.\n\nThree truncations of one command, and the order matters: the request document (days ago, committed), then the reviewer's head -1, then my tail -3. The last two were caught within an hour by each other. The first would have misled every future reviewer identically and was caught only because someone finally tried to confirm the claim rather than read it.\n\nFIXED:\n- §2 quotes the output in full, names the overdetermination, and makes the DOWNGRADE the argument: force schemaVersion to 1, change nothing else, and the frozen validator still refuses on the role alone. One violation, and it is the vocabulary.\n- check.py gains claim 1b, performing that downgrade in a temp copy every run — code, not a quotation. Temp dir deliberately, because validate_presence_v2.py globs *.json in its own directory and an invalid snapshot there would fail the suite it supports.\n- §4.1 asks for the thing at issue and says explicitly that 'my parser accepts them' is a useful answer that would make the request unnecessary. A confirmation request that cannot receive no is not a request.\n- §4.2 states that all four checks exercise the PRODUCER and that there is no v2 consumer on the gateway, so confirming it is a forward commitment rather than an attestation. The reviewer's caveat, in their terms.\n- The gateway's presence.go:52/53/426 reading is recorded as their finding: designer already a runRole there and new only as a repoRole, image-gen new on both sides, both closed maps by exact membership, renderable by enumeration, no renaming needed.\n\nVERIFIED THE NEW CHECK CAN FAIL rather than assuming it. Re-pointed check.py at a fixture set whose roles are all legal in v1: claim 1b goes red and names the reason, WHILE CLAIM 1 STILL PASSES. The vacuity demonstrated rather than argued.\n\nFrozen v1 validator and test_validator.py untouched and still green — the standing rule for this contract is that if either needed editing, the design has leaked into the freeze."}]
blockedReason: "Gateway side COUNTERSIGNED today; producer flip held deliberately, and this is now waiting on their commit rather than on any work here.\n\nThe gateway coordinator (Ryan delegated that side to them directly, in answer to a direct question) answered all four points of the request with evidence rather than assertion:\n\n  §4.1  Ran the DOWNGRADE against the real Go parser, not their reading of it: with\n        schemaVersion forced back to 1, presence.Parse still refused —\n        'presence snapshot invalid: k3n8vq2a: repoRole designer'. That is the\n        confirmation my four checks structurally could not produce, because all four\n        exercise the producer.\n  §4.2  TestV2ConsumerStillParsesEveryV1Fixture runs the new parser over the whole\n        committed v1 corpus: 7 accepted unchanged. My §4.2 caveat — that confirming it\n        was a forward commitment about a parser that did not exist — is discharged.\n  §4.3  designer and image-gen added BY ENUMERATION, in separate v2 tables rather than\n        merged into the frozen v1 sets, so the v1 vocabularies cannot drift by a one-line\n        edit. No renaming needed.\n  §4.4  The gateway accepts 1..2, so either order works once their code is in.\n\nThey also verified their own frozen-v1 test can FAIL: with the version guard removed it\ngoes red naming the reason, green when restored.\n\nONE DELIBERATE DEVIATION FROM WHAT I ASKED, and it is better: the opening is VERSION-SCOPED.\nA v1 snapshot naming designer or image-gen stays invalid. Merging the sets unconditionally\nwould make 'v2 opens the vocabulary' indistinguishable from 'the vocabulary was never\nclosed', and would retroactively legalise values no v1 producer was ever allowed to emit.\nConfirmed as the contract.\n\nWHY THIS IS STILL BLOCKED. Their tree is UNCOMMITTED, pending an unrelated containment\ncutover from another session. Countersigned-but-uncommitted is written, not landed.\nEmitting schemaVersion 2 against acceptance that exists only in a working tree is exactly\nthe drift the producer guard test exists to prevent — and the guard cannot protect against\nit, because flipping the producer means changing the thing the guard guards.\n\nWHAT REMAINS, four edits in one commit, when they confirm the commit landed:\n  presence.mjs:355   schemaVersion:1 -> 2\n  presence.mjs:25    ROLES gains image-gen\n  the repoRole set   opens to designer and image-gen\n  topology-presence-header.test.mjs:229  the 'has NOT started emitting v2' guard inverts"
evidenceSources: {".bytedesk/task-management/evidence/TM-137-TM137.md":{"source":"/tmp/claude-1000/-home-ryan-Documents-GitHub-ByteDeskAI-bytedesk-marketplace/2ee26155-9e57-4cf8-8bc4-a8379f88e5a4/scratchpad/TM137.md","sha256":"140a3e4a5f4208636ec507cb98387dedc163d525b536e47a12be5b3f789fc794","bytes":3798,"at":"2026-09-10T06:04:54.685Z"},".bytedesk/task-management/evidence/TM-137-PRODUCER-V2.md":{"source":"/tmp/claude-1000/TM-137-PRODUCER-V2.md","sha256":"ae5e82b39d365b38d4d7ef8bd3f736b29282be1a8c506c7b3be2cb046ea34168","bytes":3240,"at":"2026-09-10T23:24:33.802Z"}}
parkedReason: "Producer flip COMPLETE and sent; parked pending the integrator's merge, not pending any work.\n\nBranch tm/TM-137-producer-v2, commit 19c4e0a, rebased on 8630288. Verified NOT an ancestor of origin/main, and main still reads schemaVersion:1 — so this waits on a merge, and marking it done would be a claim the repository does not support.\n\nBoth acceptance criteria are ticked with evidence attached (TM-137-PRODUCER-V2.md).\n\nThe gate that held this for most of the epic is satisfied: the gateway's acceptance is COMMITTED, not merely countersigned. Verified in their repository rather than from their message — bd8cefc0 on develop, presenceSchemaVersionMin 1 / Max 2, and the role tables genuinely version-scoped, admitting a v2 role only when schema >= 2, so a v1 snapshot naming designer or image-gen stays invalid.\n\nWhy the hold had to be a judgement rather than a check: the producer guard test could not have prevented a premature flip, because flipping the producer IS changing the thing it guards.\n\nGates on the rebased tree, run there rather than carried forward from an earlier green:\n  topology unit    383 pass, 0 fail\n  full unit        560 tests, 556 pass, 4 skipped, 0 fail\n  frozen v1        validator + test_validator.py pass, presence-v1/ untouched by the branch\n  presence-v2      check.py ok, claim 1b included\n  build:check 0, roadmap:check 0\n\nFalsifiability pre-checked rather than assumed: reverting only the wire version to 1 while leaving the vocabularies open turns both frozen-validator-refuses assertions RED, and restoring it turns them green.\n\nNote on this note: its first version was mangled by the shell. Backticks around a code expression were command-substituted, so the sentence read \"version-scoped via , so a v1 snapshot\" with the substance deleted — and tm reported \"TM-137 parked\" either way. The confirmation was about the write succeeding, not about the content arriving. Caught by reading the record back rather than trusting the success line.\n\nUnparks the moment the branch merges; closing it closes EP-018."
closed: "2026-09-10T23:29:04.962Z"
---

Frozen v1 section 3 pins repoRole to lead|reviewer|member and says Team lead is rendered from repoRole===lead and from nothing else, and runRole lacks image-gen. So a standing designer or image-gen collapses to member and the gateway header cannot label it — which is exactly what the operator asked to see. This is a genuine wire-format change and therefore schemaVersion 2, negotiated with the gateway exactly as v1 was. Do not promise the operator a header that labels a standing designer before this lands.