---
id: "TM-135"
kind: "task"
status: "in_progress"
created: "2026-09-09T21:32:31.188Z"
board: "bytedeskai/bytedesk-marketplace"
title: "agent-orchestration: idle dispatch and provider-quota failover"
epic: "EP-018"
acceptance: [{"text":"Idle dispatch is a new backend (task-management/lib/dispatch/idle.mjs) plus a pool preference, NOT a second scheduler. Zero edits to dispatch/index.mjs: a backend is called after the claim and after provisioning, so every ordering invariant holds for free and a refusal rolls back through the existing path that never releases a pre-existing claim.","done":false},{"text":"Arbitration reuses the record that already owns who owns this task: management.mjs gains assignTaskToAgent and releaseAssignment, the management record grows an assignee, and the IDLE CHECK HAPPENS INSIDE THE SAME CRITICAL SECTION AS THE WRITE. The census is a hint; the assignment record under withLock is the authority. Check idle in the pool and assign in the backend and you ship double assignment.","done":false},{"text":"The worktree is still provisioned through tm — management.mjs ownedTask requires one and a standing agent's cwd is its own agent directory by design. One worktree per task, unchanged.","done":false},{"text":"collectIdle's completion signal is the standing-mailbox reply, NOT session death — the standing session outlives the task, which is the whole point. It routes through the unmodified recordResult so the downgrade rule and park-never-strand rule apply byte-identically, and a terminal result releases the assignment.","done":false},{"text":"CAP-0002 gets better by one deliberate line: the handoff carries a real TM_SESSION_ID and spawn asserts the session is non-null, so this path always writes an owned claim and adds zero new null-session claims. Write CAP-0002's body up — it is an empty stub whose root cause exists only in TM-127 comments.","done":false},{"text":"The quota signature stays in failure_patterns, where 'usage limit' already is and where it survives withoutPaths. attention_patterns is the wrong home and the ordering proves it: attention is checked FIRST because it means a human must press a key here, and quota exhaustion is not answerable at the keyboard.","done":false},{"text":"Detection is a supervise-tick tmux server-side subscription via tmuxFailureTrigger, so the server pushes and we capture only when the trigger fires — the productized form of 82 ad-hoc capture-pane probes, costing nothing on quiet panes. Detection writes an incident and RESTARTS NOTHING.","done":false},{"text":"failover.consent is ask (default), auto or never. ask rings the lead with the approval command and is the ONE unavoidable human turn in the whole design; auto applies AND ANNOUNCES, because the operator's rule forbids silent substitution and an operator who sets auto consented in advance in config. failoverAgent asserts the incident is open and names this agent and this provider.","done":false},{"text":"False-positive defences are all three and all required, because an agent working on THIS feature will put the signature on its own screen: the match must still be present on a second capture at least 2s later, the pane must be dead or the agent must fail a nonce probe, and ask is the default so a false positive costs one message not one provider.","done":false},{"text":"Three things survive a failover and are documented separately: the work does (same pane, same worktree — but re-bootstrap must tell the new provider to orient); the conversation does NOT (a different CLI has a different memory, which is why unanswered messages are re-delivered); the claim does via startHeartbeat, with the named ceiling that claimTtlMinutes defaults to 240 minutes against a five-hour quota window.","done":false}]
evidence: []
commits: ["a5d550e","ef33efc","74fdb5f","27625ae","4756b5c","9d7e5b6","a471883","1069713","344a8a6"]
blockedBy: ["TM-127","TM-131","TM-132"]
blocks: []
actor: "main"
session: "e01dd923-50ea-45d8-9911-b9d5faed94bd"
branch: "tm/TM-135-idle-dispatch-quota-failover"
worktree: "/home/ryan/Documents/GitHub/ByteDeskAI/bytedesk-marketplace/.bytedesk/worktrees/TM-135-dispatch"
updated: "2026-09-10T01:53:16.988Z"
comments: [{"author":"main","ts":"2026-09-09T23:40:04.160Z","text":"In progress with a live teammate, spawned this session as part of the EP-018 close-out wave. Nothing committed to its branch yet; work is in flight in its own worktree. Deliberately not merged or closed early: every worker this session returned at least one finding that changed the outcome, and two of those were bugs that a green diff and a passing test both agreed with — an unreachable retry rung whose stub reported an empty composer forever, and a brief instruction that would have granted the same cutover slot to two agents. The lead session owns the merge, the gates and the closure."},{"author":"main","ts":"2026-09-10T01:21:07.823Z","text":"Reclaimed by the mainline integrator at the conductor's request. The record said parked / session-ended e01dd923 / 0-of-10 AC. Two of those three were wrong.\n\nVerified before writing: the branch tm/TM-135-idle-dispatch-quota-failover exists in the worktree .bytedesk/worktrees/TM-135-dispatch with two commits ahead of main — a5d550e (\"part A (salvaged): idle dispatch backend, assignment arbitration, CLI wiring\") and ef33efc (\"review of the salvaged half — invariant holds, gates still unverified\"). They add task-management/lib/dispatch/idle.mjs (the artifact AC1 names by path), extend topology/lib/management.mjs by 160 lines (AC2 arbitration), and wire the CLI. The worktree also has uncommitted work in management.mjs and dispatch/collect.mjs right now, so the session is live, not ended.\n\nCorrected: status parked → in_progress; session restored to e01dd923; branch and worktree pointed at the real ones rather than main. The commit refs were 8beb169 and a05c993 — a tmux test-isolation fix and my own board-reconciliation commit, neither related to this task, both auto-stapled by the TM-146 hook bug. Replaced with the two real ones.\n\nNOT changed: the acceptance criteria stay 0-of-10. The branch's own review says the gates are unverified, and I am the integrator, not this task's reviewer — ticking criteria I have not verified would put a false claim behind the store's evidence gate. The worker or its reviewer accepts them.\n\nNote for whoever picks this up: `tm start` stamped actor/session/branch/worktree with MY session and the main checkout, and I corrected all four by hand. Reclaiming another session's task through `tm start` silently reassigns ownership — worth a task of its own."},{"author":"main","ts":"2026-09-10T01:26:24.014Z","text":"Routed report from the worktree dispatcher, recorded by the integrator. Status stays in_progress — the dispatcher confirms it is correct, and done/block/park would all be false while child agent dispatch2 has live uncommitted work in .bytedesk/worktrees/TM-135-dispatch. The dispatcher declined to write the shared store itself and declined to arm `tm override`, which was right.\n\nBranch tm/TM-135-idle-dispatch-quota-failover now at 74fdb5f, three commits: a5d550e (salvaged half, committed unreviewed to save a crashed worker output), ef33efc (the dispatcher own source review — headline: the double-assignment invariant HOLDS, because assignTaskToAgent does the census read inside the same withLock that writes the assignment), 74fdb5f (gate evidence). Commit refs corrected to those three; the record had picked up eac8ae7 and c87d2f9, two of the integrator own board commits, via the TM-146 hook bug — twice during this one turn.\n\nCORRECTION TO THE GATE EVIDENCE, and it matters. 74fdb5f reports one task-management failure, `not ok 206 - the handshake identifies the code, not merely dev`, as PRE-EXISTING, verified against a control run on a5d550e^ in an independent git archive tree. The control does not support that conclusion. Both trees are `git archive` extracts, and a git archive tree has no .git directory. The test asserts the MCP handshake does not answer `dev`, and its own comment says how it is meant to pass: \"a source checkout asks git\". With no .git to ask, the handshake answers dev and the assertion fails by construction.\n\nVerified rather than argued: current main passes this test in a real checkout (task-management unit suite 1350 pass, 0 fail), and the SAME test fails when main is extracted with `git archive` into /tmp and run there — 29 pass, 1 fail, the handshake test. So the failure is an artifact of the archive-based method, not a defect in a5d550e and not a defect in main.\n\nThis is the second occurrence of the confounder the evidence document itself names. It warns that an earlier worker symlinked one node_modules into both trees and wrongly concluded pre-existing, and states \"a control that shares the confounder is not a control\". Both archive trees share the missing .git identically. The method is otherwise sound and the isolation discipline is genuinely better than what came before — this one confounder just survived it.\n\nConsequence: there is NO unowned defect here and the integrator has filed no task for it. The dispatcher flagged `not ok 206` as an untracked defect someone should own; nobody should, because it is not one. What is worth owning is the harness gap: gates run from archive trees will fail this test forever and read as a real regression to the next person. Either run gates from a real checkout or clone, or give the test a documented archive-tree skip.\n\nStill open in the dispatcher scope, unchanged: collectIdle and its routing entry, the dispatch.preferIdle pool preference, and all of Part B (quota.mjs, failover.consent, the false-positive defences). Not verified at 74fdb5f by anyone: build:check and roadmap:check, which need node_modules an archive tree lacks — the same limitation as above.\n\nNoted for whoever schedules it: the dispatcher asks that a5d550e get an INDEPENDENT read-only re-review, and explicitly asks that ef33efc not substitute for it, saying it would rather be overturned by someone else than trusted because it wrote it. The integrator is not that reviewer and has not reviewed a5d550e."},{"author":"main","ts":"2026-09-10T01:29:28.511Z","text":"Revision advanced to 27625ae. The dispatcher retracted the pre-existing-failure conclusion in its own gate evidence after the integrator disproved it, and committed the retraction inline — 43 lines added, none deleted, so the original section and the error stay legible to the next reader rather than being quietly removed. That is the right way to correct a record.\n\nThe retraction states plainly that there is no unowned defect, that nobody should look for an owner, and that TM-149 is the real issue. It also carries forward that build:check and roadmap:check remain unverified at this revision for the same root cause: an archive tree has no node_modules either.\n\nWhat still stands from that gate run, none of it dependent on .git: topology 289/289, both frozen presence validators passing unmodified, frozen fixtures untouched.\n\nProgress: dispatch2 has landed collectIdle. Still open in the dispatcher scope — the dispatch.preferIdle pool preference, and all of Part B: quota.mjs, failover.consent, the false-positive defences. The dispatcher has undertaken to send reviewed revisions and to say so loudly where it cannot, as it did for a5d550e.\n\nCommit refs set to the four real ones. They had been re-stapled with this session own board commits twice already; TM-146 covers that."}]
parkedReason: "session ended (e01dd923-50ea-45d8-9911-b9d5faed94bd)"
touches: ["agent-orchestration/docs/quota-failover.md","agent-orchestration/tests/unit/topology-idle-assignment.test.mjs","agent-orchestration/tests/unit/topology-quota.test.mjs","agent-orchestration/topology/lib/quota.mjs","task-management/tests/unit/dispatch-idle.test.mjs"]
---

Two halves of the same tick. Idle dispatch hands a ready task to an already-live idle enrolled agent instead of launching a new one; quota failover detects a provider usage-limit mid-run, which today nobody looks for after startup. Both implementers died on 'Error: [provider.auth_error] 403 You have reached your 5-hour usage limit' and were recovered only by a human authorising a manual Codex takeover. Design detail is in the approved plan under .bytedesk/task-management/plans/.