# Per-user fleet state — never commit
.claude-sessions/
.claude/plugins/
# .bytedesk/task-management/ IS committed — the task store is shared work, and it carries
# its own .gitignore for the machine-local parts (index.json, state.json, dashboard.*).
.bytedesk/worktrees/
**/.in_use/
.claude/worktrees/
notifications.fifo
notify.log
notify.config.toml
*.events
*.events.offset
*.events.err

# Local agent stores
ruvector.db
ruvector.db.lock
agentdb.rvf
agentdb.rvf.lock
.swarm/
.claude-flow/

# Editor / OS noise
.DS_Store
*.swp
*.swo
*~

# Test artifacts
*.bak
.playwright-mcp/
*.png

# design-patterns plugin — Python artifacts
__pycache__/
*.pyc

dashboard.pid
dashboard.port

# PM plugin — workspace data directories (created by pm_init in-repo during dev/test)
# The real workspace lives in the user's project directory, not here.
project-management/.pm/
**/.pm/pm.db
**/.pm/pm.db-shm
**/.pm/pm.db-wal
**/.pm/events.jsonl
**/.pm/dashboard.pid
**/.pm/dashboard.port

# Claude telemetry and session artifacts
.claude/telemetry/
.claudeignore