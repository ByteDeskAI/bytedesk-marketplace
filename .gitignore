# Per-user fleet state — never commit
.claude-sessions/
.claude/worktrees/
notifications.fifo
notify.log
notify.config.toml
*.events
*.events.offset
*.events.err

# Local agent stores
ruvector.db
ruvector.db.lock
agentdb.rvf
agentdb.rvf.lock
.swarm/
.claude-flow/

# Editor / OS noise
.DS_Store
*.swp
*.swo
*~

# Test artifacts
*.bak
.playwright-mcp/
